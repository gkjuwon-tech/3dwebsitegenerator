{
  "name": "hero-forge",
  "version": "0.1.0",
  "private": true,
  "description": "AI-composed high-end 3D interactive hero generator. Decomposes a prompt into main model, ground background, procedural sky, camera/interaction timeline and lighting, then plays it in a cinematic Three.js runtime.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @hero/studio",
    "build": "npm run build --workspace @hero/studio",
    "preview": "npm run preview --workspace @hero/studio",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
